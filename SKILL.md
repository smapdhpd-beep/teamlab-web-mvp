# teamlab-web-mvp — 技术规则与设计手册

> 记录项目开发中沉淀的可复用技术决策、性能基准与视觉规范。

---

### 粒子交互物理规则（来源：Step 2 排斥交互调优）
- **必须拆分 `baseVel` 与 `vel`**：基础漂移速度永久保存，交互力只 perturb `vel`，每帧 `lerp` 回归。禁止对整体速度做统一衰减（如 `vel.mult(0.98)`），否则粒子会停滞。
- **排斥力度量级约束**：`repelStrength` 应接近或低于基础漂移速度量级（当前基准 `0.6` vs 基础速 `0.3~0.7`）。若反馈"烟花感/弹射感"，优先降 `repelStrength` 再降 `maxVelocity`。
- **速度上限比例**：`maxVelocity` 建议不超过基础均速的 `2~3` 倍（当前 `1.5` vs `0.5` 均速），封顶过高会产生弹道感，破坏 teamLab 水流感。

### 后处理 blendMode 执行规则（来源：Step 3 发光效果实现）
- **background() 必须在 BLEND 模式下执行**：`blendMode(ADD)` 下调用 `background()` 会让半透明黑以加性方式叠加，导致拖尾失效、画面持续过曝发白。
- **blendMode 统一在 draw() 里切换，禁止在每个粒子 display() 中切换**：Canvas `globalCompositeOperation` 状态切换开销极高，每帧只切 2 次（进 ADD / 回 BLEND）。
- **执行顺序铁律**：`BLEND` → `background()` → `ADD` → 画所有发光层 → `BLEND` → 画 UI/文字。

### ADD 模式光晕绘制规则（来源：Step 6 吸引光晕调优）
- **禁止用硬边几何图形（circle/rect）叠加模拟光晕**：在 `blendMode(ADD)` 下，多个不同半径的硬边圆叠加会产生可见环纹（同心圆伪影），且恒定 alpha 每帧叠加会导致持续过曝。
- **必须使用 Canvas 2D 径向渐变**：`createRadialGradient` 实现从中心到边缘的数学平滑 falloff，无环纹、无硬边。
- **长时间可见的光晕必须加呼吸波动或降低基础 alpha**：ADD 模式下残影衰减慢于叠加速度，恒定绘制必然过曝。通过 `sin(frameCount * freq)` 调制 alpha，制造有机脉动感。

### 体感输入 EMA 平滑规则（来源：Step 6 手控延迟调优）
- **必须包含"快速移动直通"机制**：纯 EMA 在位移大时滞后明显。当连续帧位移超过阈值（当前基准 20px），应临时提高 α 到 0.8+，实现近零滞后跟手；静止后再降 α 防抖。
- **静止判定应严格且快速**：阈值过高或帧数过多会导致手刚停就 jitter。当前基准：位移 < 2px 连续 3 帧进入深度平滑（α=0.08）。

### 波纹系统性能规则（来源：Step 7 涟漪实现）
- **必须限制同时存在波纹数**：`maxRipples ≤ 3`，防止连点导致 O(n×m) 双重循环拖垮帧率。
- **必须做 AABB 预筛选**：在 `dist()` 之前先检查 `|dx| > radius + ringWidth || |dy| > radius + ringWidth`，轴对齐包围盒外的粒子直接跳过，避免无意义的平方根计算。
- **影响模型用"波前区域"而非细环**：`|d - radius| < ringWidth` 配合力度衰减，比恰好落在环上的判断更有体感，粒子响应更明显。

### 波纹视觉规则（来源：Step 7 涟漪视觉调优）
- **波纹必须在 BLEND 模式下绘制，禁止在 ADD 下画白色描边**：ADD 模式下白色圆环会和粒子发光叠加过曝，产生刺眼亮边。应在 `background()` 之后、`ADD` 画粒子之前，用 `BLEND` 画淡色细环。
- **波纹颜色必须融入主色调，禁用纯白**：当前基准淡青色 `[100, 220, 220]`，让波纹成为画面的一部分而非跳脱元素。

### 体感缺少"点击"手势时的 fallback 规则（来源：Step 7 Hand 触发设计）
- **优先用"悬停蓄力"替代复杂手势识别**：当 MediaPipe 只输出指尖位置而无捏合/点击检测时，让指尖悬停 0.8s 自动触发一次动作，配合 preview 窗口倒计时提示。比"戳""挥"等动态手势稳定得多，用户可发现性也更高。

### 粒子连线性能规则（来源：Step 8 光之网实现）
- **必须用空间索引，禁止 O(n²) 暴力遍历**：1500 粒子两两比较约 112 万次距离计算，不可接受。当前基准：50×50 网格 + `Map` 存储，每帧 `clear()` 重建，只查周围 9 格，降至约 12 万次检查。
- **必须去重**：`other.index > p.index`（或等效逻辑），避免 A→B 和 B→A 画两次同一条线。
- **必须限制单粒子连线数**：每粒子最多连 3 条最近邻居，不是见人就连。防止局部粒子密集区线条爆炸。
- **网格必须用 `Map` 而非普通对象**：`grid.clear()` + `grid.set()` 比 `grid = {}` 每帧新建对象的 GC 压力更小。

### 连线视觉规则（来源：Step 8 光之网调优）
- **同色连线，禁止跨色**：跨色 `lerpColor` 会产生灰紫脏色。粒子按 `colorType`（0粉/1青/2金）分组，只连接同类型，形成三色独立交织的网络。
- **连线 alpha 必须同步双方生命周期**：`alpha = baseAlpha × min(lifeAlpha1, lifeAlpha2)`，让濒死粒子的连线同步淡出，避免"透明粒子拉着亮线"的割裂感。
- **连线在 BLEND 下绘制，weight ≤ 0.5**：连线是结构层不是发光层，应如蛛丝般隐约可见。当前基准：BLEND、α70、weight 0.5。

### 流动场架构规则（来源：Step 9 流动场实现）
- **方向与速率必须拆分为独立通道**：flowField 只扰动方向向量，速率大小由个体恒定值（如 `baseSpeed`）或独立慢变噪声控制。禁止每帧对速度大小做随机重采样，否则轨迹失稳、破坏有机感。
- **扰动必须在场更新层注入，查询函数保持只读**：交互产生的局部偏移应在 `update()` 中修改场数据，不应在 `getForce()` 中耦合交互逻辑。保持查询函数纯只读，避免每粒子重复计算与交互点的距离。
- **空间采样禁止最近邻**：`floor(x / resolution)` 会导致粒子跨网格线时力跳变。必须用双线性插值（bilinear interpolation）按 sub-grid 坐标加权四角，确保力向量连续平滑变化。
- **一维化存储优先于二维数组**：`new Float32Array(cols * rows)` 按 `idx = row * cols + col` 索引，复用同一块内存。禁止每帧 `this.field = []` 后逐行 `push`，避免 GC 颠簸。
